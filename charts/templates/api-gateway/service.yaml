apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: default
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
    - name: api-gateway
      port: 8000
      targetPort: 8000
      nodePort: 30080
---

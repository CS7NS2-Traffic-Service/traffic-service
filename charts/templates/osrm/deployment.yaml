apiVersion: apps/v1
kind: Deployment
metadata:
  name: osrm
  namespace: default
  labels:
    app: osrm
spec:
  selector:
    matchLabels:
      app: osrm
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: osrm
      labels:
        app: osrm
    spec:
      containers:
        - name: osrm
          image: ghcr.io/cs7ns2-traffic-service/traffic-service/osrm-ireland:latest
          imagePullPolicy: Never
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          ports:
            - containerPort: 5000
              name: osrm
          livenessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            periodSeconds: 10
---

apiVersion: v1
kind: Service
metadata:
  name: routes-service
  namespace: default
spec:
  selector:
    app: routes-service
  type: ClusterIP
  ports:
    - name: routes-service
      port: 8083
      targetPort: 8083
---

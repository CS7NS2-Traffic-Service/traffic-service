apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: default
spec:
  selector:
    app: messaging
  type: ClusterIP
  ports:
    - name: messaging-service
      port: {{ .Values.ports.messagingService }}
      targetPort: {{ .Values.ports.messagingService }}
---

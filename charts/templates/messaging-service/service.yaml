apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: default
spec:
  selector:
    app: messaging
  type: ClusterIP
  ports:
    - name: messaging-service
      port: 8085
      targetPort: 8085
---

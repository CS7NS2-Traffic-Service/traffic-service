apiVersion: v1
kind: Service
metadata:
  name: driver-service
  namespace: default
spec:
  selector:
    app: driver-service
  type: ClusterIP
  ports:
    - name: driver-service
      port: 8081
      targetPort: 8081
---
